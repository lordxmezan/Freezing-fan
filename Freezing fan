<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Freezing Fan with Cold Sound</title>
  <style>
    body {
      margin: 0;
      background: #111;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      color: white;
      font-family: sans-serif;
      overflow: hidden;
    }

    .fan {
      width: 150px;
      height: 150px;
      background: radial-gradient(circle, #888 20%, #444 70%);
      border-radius: 50%;
      position: relative;
      margin-bottom: 30px;
      transition: all 2s ease;
    }

    .fan.freeze {
      background: radial-gradient(circle, #00f 20%, #009 70%); /* Ice Blue color */
      box-shadow: inset 0 0 20px rgba(255, 255, 255, 0.3);
    }

    .blade {
      position: absolute;
      width: 10px;
      height: 60px;
      background: #ccc;
      top: 0;
      left: 50%;
      transform: translateX(-50%);
      border-radius: 5px;
    }

    .blade:nth-child(2) {
      transform: translateX(-50%) rotate(120deg);
      transform-origin: 50% 75px;
    }

    .blade:nth-child(3) {
      transform: translateX(-50%) rotate(240deg);
      transform-origin: 50% 75px;
    }

    .spinning {
      animation-name: spin;
      animation-timing-function: linear;
      animation-iteration-count: infinite;
    }

    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    button {
      padding: 10px 20px;
      font-size: 16px;
      background: #444;
      color: white;
      border: none;
      border-radius: 5px;
      margin-bottom: 15px;
    }

    button:hover {
      background: #666;
    }

    .slider-container {
      width: 200px;
      text-align: center;
    }

    input[type="range"] {
      width: 100%;
    }

    /* Snowfall effect */
    .snowflake {
      position: absolute;
      color: #fff;
      font-size: 30px;
      opacity: 0.7;
      pointer-events: none;
      animation: snowfall 10s linear infinite;
    }

    @keyframes snowfall {
      from { top: -10px; }
      to { top: 100vh; }
    }

  </style>
</head>
<body>

<div id="fan" class="fan">
  <div class="blade"></div>
  <div class="blade"></div>
  <div class="blade"></div>
</div>

<button id="toggleBtn">Start Fan</button>

<div class="slider-container">
  <label for="speedSlider">Speed: <span id="speedValue">0.6</span>x</label>
  <input type="range" id="speedSlider" min="0.2" max="3" step="0.1" value="0.6" disabled>
</div>

<!-- Cold Fan Sound -->
<audio id="fanSound" loop>
  <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mp3">
  Your browser does not support the audio element.
</audio>

<!-- Snowflakes -->
<script>
  // Function to create snowflakes
  function createSnowflakes() {
    const snowflakeCount = 30;
    for (let i = 0; i < snowflakeCount; i++) {
      const snowflake = document.createElement('div');
      snowflake.classList.add('snowflake');
      snowflake.textContent = '❄';
      snowflake.style.left = `${Math.random() * 100}%`;
      snowflake.style.animationDuration = `${Math.random() * 5 + 5}s`;
      snowflake.style.fontSize = `${Math.random() * 20 + 15}px`;
      document.body.appendChild(snowflake);
    }
  }

  createSnowflakes();

  const fan = document.getElementById("fan");
  const button = document.getElementById("toggleBtn");
  const slider = document.getElementById("speedSlider");
  const speedDisplay = document.getElementById("speedValue");
  const fanSound = document.getElementById("fanSound");

  let spinning = false;
  let freezing = false;

  // Fixed speed of 0.6
  const fixedSpeed = 0.6;

button.textC
